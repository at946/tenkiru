version: '3'

services:
  app:
    image: node:20.1.0
    working_dir: /app
    command: yarn dev
    volumes:
      - .:/app
    ports:
      - 3000:3000

  playwright:
    build: playwright
    volumes:
      - ./playwright:/playwright
    depends_on:
      - app
    profiles:
      - extra
