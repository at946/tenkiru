pre-commit:
  parallel: true
  commands:
    check:
      run: pnpm run biome:fix {staged_files}
      stage_fixed: true
pre-push:
  parallel: true
  commands:
    check-typescript:
      run: pnpm type:check
